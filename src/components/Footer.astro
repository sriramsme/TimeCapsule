<footer
    class="fixed bottom-4 left-1/2 -translate-x-1/2 z-50 pointer-events-none"
>
    <div
        class="pointer-events-auto
      flex items-center gap-2
      px-4 py-2
      rounded-full
      backdrop-blur-md
      bg-muted/60
      border border-border/50
      shadow-lg shadow-black/5
      text-muted-foreground
      text-xs sm:text-sm
      max-w-[92vw]
      whitespace-nowrap"
    >
        <!-- Lock icon -->
        <svg
            class="w-3.5 h-3.5 shrink-0 opacity-70"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
        >
            <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"
            ></path>
        </svg>

        <!-- Privacy text (responsive) -->
        <span class="hidden sm:inline">
            Your data never leaves your browser.
        </span>

        <span class="sm:hidden"> Private & local. </span>

        <!-- Divider -->
        <span class="opacity-40">•</span>

        <!-- Always visible credit -->
        <span class="whitespace-nowrap">
            Made with <span class="text-primary">❤️</span>
            by
            <a
                href="https://srirams.me"
                target="_blank"
                class="underline hover:text-foreground transition-colors cursor-pointer"
            >
                @sriramsme
            </a>
        </span>
    </div>
</footer>
